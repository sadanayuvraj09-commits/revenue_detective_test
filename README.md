# s={1,2,3,4}
# s1={1,2,4,5}
# u=s.union(s1)
# print(u)

# l=[{1,2,3},{2,3,4},{3,4,5},{2,3,5}]
# max_index=None
# max=l[0].intersection(l[1]).intersection(l[2])
# for i in range(len(l)):
#     for j in range(i+1,len(l)):
#         for k in range(i+2,len(l)):
#             if l[i].intersection(l[j]).intersection(l[k])>len(max):
#                 max= 
